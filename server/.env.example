# Clé API Claude (Anthropic) — https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Domaines autorisés à appeler ce backend, séparés par des virgules
# (le domaine GitHub Pages du site + localhost pour le dev)
ALLOWED_ORIGINS=https://skenplus.github.io,http://localhost:5500

# Port d'écoute du serveur (Render le définit automatiquement)
PORT=3000
